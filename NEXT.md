# 📍 จุดกลับมาทำต่อ

สถานะ ณ 15 ส.ค. 2569 · **205 เทสต์ผ่าน · typecheck สะอาด · ติดตั้งจาก GitHub ได้แล้ว**

## ทำอะไรไปแล้ว

| | |
|---|---|
| `packages/core` | โครงแคตตาล็อกตาม ม.39 · กฎการชี้ขาดสามข้อ · อ่าน/เขียน YAML โดยคอมเมนต์ของคนไม่หาย · ตัวเดาจากชื่อฟิลด์ 35 กฎ · **ตารางเทียบกับ Fideslang** |
| `packages/prisma` | ตัวอ่าน `.prisma` ที่ให้เลขบรรทัดจริง · แยกกุญแจนอกออกจากตัวข้อมูล · อ่าน `@pii` / `@not-pii` |
| `packages/detect-th` | ตัวตรวจค่าจริงของไทย 12 ชนิด · `Redactor` ที่ให้ตัวแทนคงที่และแปลงกลับได้ · ไม่มี dependency |
| `packages/cli` | `init` / `sync` / `baseline` / `status` / `scan` / **`ropa`** / **`semgrep`** / **`export`** · **`--format sarif`** · ตัวเขียน `.xlsx` ที่เขียนเองไม่มี dependency |
| `packages/plugin` | ปลั๊กอิน Claude Code — ฮุกสามตัว + คำสั่ง `/arak:*` + `bin/arak.mjs` ที่ bundle แล้ว |
| `action.yml` | **GitHub Action** — รัน bundle ที่ commit ไว้ ไม่ต้อง install/build ระหว่างสายพานทำงาน |
| `.pre-commit-hooks.yaml` | **ฮุก pre-commit สามตัว** — `arak-status` · `arak-scan` · `arak-catalog-in-sync` |
| `.github/workflows/ci.yml` | เทสต์ + typecheck + build + **ตรวจว่า bundle ที่ commit ตรงกับซอร์ส** + Arak ตรวจตัวเอง |
| `docs/landscape.md` | **ผลสำรวจเครื่องมือใกล้เคียง 11 ตัว** พร้อมเหตุผลว่าหยิบอะไรมา ทิ้งอะไร |
| `docs/ci.md` | วิธีเอาไปวางใน GitHub Actions / pre-commit / GitLab CI |
| `.claude-plugin/` | แคตตาล็อก marketplace ติดตั้งจากพาธในเครื่องได้เลย |
| `examples/demo-app` | สคีมาสมมติที่ใช้เป็นสนามทดสอบ · แคตตาล็อกปิดงานครบ 14 มาร์ก |

## รอบวันที่ 15 ส.ค. — ตามผลสำรวจใน `docs/landscape.md`

1. **แก้บั๊กที่อันตรายที่สุดของโครงการ** — `arak scan <โฟลเดอร์>` และ `arak scan "<glob>"`
   เคยพิมพ์ `อ่านไม่ได้` แล้ว **จบด้วยรหัส 0 พร้อมข้อความว่าไม่พบข้อมูลส่วนบุคคล**
   ด่าน CI ที่เขียน `arak scan src/` จึงเขียวตลอดกาลทั้งที่ไม่ได้อ่านอะไรเลย
   ตอนนี้ขยายโฟลเดอร์กับ glob ได้จริง และพาธที่หาไม่เจอจบด้วยรหัส 2 ตามที่ README ประกาศไว้เอง
   เทสต์กันการถอยหลังอยู่ที่ `packages/cli/test/collect.test.ts`

2. **SARIF + Action + pre-commit** — ข้อ A ในผลสำรวจ คุ้มที่สุดแรงน้อยที่สุด
   gitleaks/semgrep/Bearer ออก SARIF กันหมดแล้ว ส่วน Arak เคยมีแค่รหัสจบ

3. **`arak ropa`** — ข้อ B · ออก `.xlsx` สามชีต · ตัวเขียน ZIP+XML เขียนเองใน `packages/cli/src/xlsx.ts`
   เพราะปลั๊กอินต้อง bundle แบบไม่มี dependency การลาก exceljs เข้ามาจะพังข้อนั้น
   **ตรวจแล้วว่าเปิดใน Excel ได้จริง** — สไตล์หัวตาราง ความกว้างคอลัมน์ ตรึงแถวหัว ภาษาไทยครบ

4. **`arak export --format fideslang`** — ข้อ C · Fideslang เป็น CC BY 4.0 จึง map ได้
   ตรง 18 หมวด ไม่ตรง 5 หมวด **ทุกช่องที่ไม่ตรงมีเหตุผลกำกับและถูกรายงานตอนส่งออก**
   เทสต์ตรวจว่าคีย์ปลายทางทุกตัวมีอยู่จริงในอนุกรมวิธาน ไม่ใช่คีย์ที่แต่งขึ้น

5. **`arak semgrep`** — ข้อ F ทางลัด · กฎ taint ที่ source มาจากแคตตาล็อก

⚠️ **ยังไม่ได้ตรวจกฎ Semgrep กับ engine จริง** — semgrep ไม่รองรับ Windows แบบ native
ที่ตรวจแล้วคือ YAML parse ได้และโครงถูกตามสเปก แต่ยังไม่เคยรัน `semgrep --config` จริงสักครั้ง
ต้องลองบน WSL หรือใน CI ก่อนเคลมว่ากฎใช้ได้

ทดสอบกับสคีมาการผลิตจริงสี่ชุดแล้ว (ระบบคลังสินค้า · ซ่อมบำรุง · บุคคล · คลินิกสมมติ)
จำนวนฟิลด์อยู่ที่ 27-43 ต่อระบบ · รายละเอียดของแต่ละระบบเก็บไว้นอก repo โดยตั้งใจ

## การตัดสินใจเรื่องฮุกที่เคาะไปแล้ว (14 ส.ค.)

1. **ความดุ** — บอกในเทิร์น แต่จบงานได้ ไม่มี `Stop` hook ที่ deny
2. **เส้นฐาน** — ของเก่าพักไว้ด้วย `arak baseline` เตือนเฉพาะของใหม่
3. **ใครมาร์ก** — Claude เติม `category` ได้ แต่ `purposes` ต้องถามคนเสมอ
4. **ทดลองที่** — ตัว Arak เอง (`.claude/settings.json`) + `examples/demo-app`

ทดสอบฮุกด้วยมือแล้วครบห้าเคส — เจอฟิลด์ใหม่ · เขียนแคตตาล็อกตอนจบเทิร์น ·
เงียบเมื่อไม่ใช่โปรเจกต์ Arak · เงียบเมื่อแก้ไฟล์ที่ไม่ใช่สคีมา · อินพุตพังแล้วไม่ระเบิด

## วิธีติดตั้ง (ทางที่ควรใช้)

```
/plugin marketplace add D:/Project/arak
/plugin install arak@arak
```
รีสตาร์ต แล้ว `/arak:setup`

ปลั๊กอินถูก bundle ให้ไม่มี dependency เลย (1.3MB 5 ไฟล์) เพราะ Claude Code
**คัดลอกเฉพาะโฟลเดอร์ปลั๊กอิน** ไปแคช และโหมด link ใช้บนวินโดวส์ไม่ได้

⚠️ **`.claude/settings.json` ของ repo นี้ยังชี้ฮุกชุดเดียวกันอยู่**
ถ้าติดตั้งผ่าน `/plugin` แล้ว ให้ลบไฟล์นั้นทิ้ง ไม่งั้นฮุกจะยิงซ้ำสองรอบ

## ทำต่อได้ทันที

1. **ยังไม่เคยเห็นฮุกทำงานในเซสชันจริง** — ที่ทดสอบคือป้อน JSON เข้า stdin เอง
   ลองที่ **`D:/Project/arak-sandbox`** ซึ่งเป็นโปรเจกต์เปล่าที่ทำไว้ให้ลองโดยเฉพาะ
   ควรได้ผล `ยังไม่ได้ตัดสิน 28 · ข้อมูลอ่อนไหว ม.26 5`

2. **ยังไม่เคยรัน GitHub Action จริง** — `action.yml` กับ `.github/workflows/ci.yml`
   เขียนเสร็จแล้วแต่ยังไม่เคยยิงบน runner จริงสักครั้ง
   จุดที่น่าจะพังก่อนเพื่อนคือ `permissions: security-events: write`
   ซึ่งรีโป private ที่ไม่มี Advanced Security จะอัป SARIF ไม่ได้

3. **ยังไม่เคยติดตั้งผ่าน pre-commit จริง** — `language: node` จะให้ pre-commit
   รัน `npm install` บนรีโปนี้ ซึ่งพึ่ง `bin` ใน `package.json` ราก
   ที่ชี้ไปยัง bundle ที่ commit ไว้ ต้องลองจริงว่าไม่มี `prepare` script มาสะดุด

4. **`--since <ref>`** — ข้อ D ในผลสำรวจ ตรวจเฉพาะที่เปลี่ยนเทียบกับ git ref
   ตรงกับปรัชญา "เตือนเฉพาะของใหม่" และทำให้ CI ของรีโปใหญ่ไม่ช้า

5. **ตัวอ่านนอกเหนือจาก Prisma** — ข้อ E · `sources:` ในคอนฟิกเป็น map ตาม `kind` อยู่แล้ว
   ลำดับที่ผลสำรวจแนะนำคือ **SQL DDL → Drizzle → TypeORM** เพราะ SQL รับ legacy ได้กว้างสุด

## ค้างที่การตัดสินใจ

- **ยังไม่ push ขึ้น GitHub** — ยังไม่ได้ตกลงว่า repo จะเป็น public ตั้งแต่แรกหรือ private ก่อน
  ถ้า public ต้องเช็คให้แน่ว่าไม่มีอะไรของลูกค้าหลุดไปใน git history (ตอนนี้สะอาด สแกนแล้ว)
- **ชื่อแพ็กเกจบน npm** — ตอนนี้ใช้ scope `@arak/*` ในเวิร์กสเปซ ยังไม่ได้จอง scope จริง
  ถ้าจองไม่ได้ให้เปลี่ยนเป็น `arak-core` `arak-detect-th` แบบไม่มี scope
- **โดเมน `arak.growgenius.co.th`** ยังไม่ได้ตั้ง

## กับดักที่เจอมาแล้ว อย่าเหยียบซ้ำ

- **แคตตาล็อกเคยถูกเขียนเป็นบรรทัดเดียว** เพราะ `fields: []` ที่ว่างทำให้ yaml ใช้ flow style
  แล้วของที่ต่อท้ายสืบทอดมาด้วย → `blockify()` ใน `catalog.ts` แก้ไว้แล้ว **ห้ามถอด**
- **กุญแจนอกทำให้ `empId` โผล่ซ้ำ 21 ตาราง** ในสคีมาระบบบุคคล → ต้องอ่าน `@relation(fields: [...])`
- **เลขผู้เสียภาษี 13 หลักผ่าน Luhn ได้ราวหนึ่งในสิบ** จึงเคยถูกจับเป็นบัตรเครดิต
- **หลักตรวจสอบบัตรประชาชนมีจุดบอด** หลักที่สามน้ำหนัก 11 → แก้แล้วสูตรจับไม่ได้เลย
  ห้ามเคลมว่า "ผ่าน checksum แปลว่าเลขมีอยู่จริง"
- **`scan` ห้ามพิมพ์ค่าจริงลง stdout** log ของ CI อยู่นานกว่าไฟล์ที่ถูกสแกน
- ตอนรัน CLI ข้ามโปรเจกต์ **cwd ของเชลล์ค้างจากคำสั่งก่อนหน้า** ให้ `cd` กลับรากทุกครั้ง
- **ฮุกต้องจบด้วยรหัส 0 เสมอ** แม้จะพัง — เครื่องมือที่ทำให้เซสชันของคนใช้พังคือเครื่องมือที่ถูกถอด
  ทุกไฟล์ใน `packages/plugin/hooks/` จึงมี `.catch()` ที่แปลงข้อผิดพลาดเป็นข้อความเตือน
- **`pnpm -r exec` พังกับแพ็กเกจที่ไม่มี tsconfig** ใช้ `pnpm -r run typecheck` แทน
  แล้วให้แต่ละแพ็กเกจประกาศสคริปต์ของตัวเอง
- **`arak scan` เคยจบด้วยรหัส 0 ทั้งที่ไม่ได้อ่านไฟล์เลย** เมื่อพาธที่ให้เป็นโฟลเดอร์หรือ glob
  แก้แล้วใน `collectFiles` ซึ่งตอนนี้คืน `unresolved` แยกออกมา **อย่าเอาไปยุบรวมกับ `skipped`**
  เพราะสองอย่างนี้คนละความหมาย — อ่านไฟล์ที่เจอไม่ได้ กับสั่งพาธที่ไม่มีอยู่
- **ตัวเขียน `.xlsx` ต้องให้ไบต์เดิมทุกครั้ง** เวลาแก้ไขใน ZIP จึงถูกตรึงที่ 1980-01-01
  ถ้าใส่เวลาจริงเข้าไป เอกสารเดิมจะ diff ไม่ได้และตรวจย้อนหลังไม่ได้
- **`buildXlsx` ต้องไม่ใส่ `<c>` ให้ช่องว่าง** Excel จะบ่นเรื่องไฟล์เสียถ้ามี cell ที่ไม่มีค่า
- **ปลั๊กอินต้อง bundle ให้ไม่มี dependency** ไม่ใช่ทางเลือก — Claude Code คัดลอกเฉพาะ
  โฟลเดอร์ปลั๊กอิน symlink ของ pnpm ไม่ตามไปด้วย และ link mode ใช้บนวินโดวส์ไม่ได้
- **`yaml` มีแต่รุ่น CommonJS สำหรับ node** พอ bundle เป็น ESM จะพัง
  `Dynamic require of "process"` → ต้องเติม `createRequire` **ใต้ shebang**
  ใส่ผ่าน `banner` ของ esbuild ไม่ได้เพราะมันไปอยู่เหนือ shebang แล้วไฟล์ parse ไม่ผ่าน
  ตัว `build.mjs` จึง post-process เอง

## วิธีลองเร็ว ๆ

```bash
cd D:/Project/arak
pnpm install && pnpm run build && pnpm test

mkdir -p .scratch/try/prisma
cp <schema ของโปรเจกต์ที่อยากลอง> .scratch/try/prisma/
node packages/cli/dist/index.js init --root .scratch/try
node packages/cli/dist/index.js sync --root .scratch/try
```

`.scratch/` อยู่ใน `.gitignore` แล้ว ใช้ลองกับ repo อื่นได้โดยไม่เขียนอะไรลง repo นั้น
